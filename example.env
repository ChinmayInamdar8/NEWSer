# Copy to .env at the repo root if you need shared tooling vars.
# App/package-specific secrets belong in that workspace's own .env
# (copy from that folder's example.env).

# Optional shared defaults
NODE_ENV=development
